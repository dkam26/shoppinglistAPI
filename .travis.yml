language: python

python: "3.6.3"
postgre: "9.4"
#To install dependences
install:
   -pip install -r requirements.txt
#To run tests  
script: 
    -nosetests --with-coverage --cover-package=my_app test_list.py
